{
  "name": "sudoku-chess",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --kill-others \"yarn client\" \"yarn server\" ",
    "build": "concurrently --kill-others \"yarn build-client\" \"yarn server\" ",
    "client": "cd client && yarn start",
    "build-client": "cd client && yarn build",
    "server": "cd server && nodemon index.ts"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
